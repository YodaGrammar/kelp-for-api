{% for type, messages in app.session.flashbag.all() %}
    {% for message in messages %}
        toastr.options = {
            "positionClass": "toast-top-right",
            "closeButton": true
        };
        toastr.info("{{ message }}");
    {% endfor %}
{% endfor %}
