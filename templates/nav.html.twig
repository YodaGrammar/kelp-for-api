{% set currentPath = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) %}

{#<nav class="navbar navbar-inverse navbar-fixed-top">#}
    {#<div class="container">#}
        {#<div class="navbar-header">#}
            {#<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"#}
                    {#aria-expanded="false" aria-controls="navbar">#}
                {#<span class="sr-only">Toggle Navigation</span>#}
                {#<span class="icon-bar"></span>#}
                {#<span class="icon-bar"></span>#}
                {#<span class="icon-bar"></span>#}
            {#</button>#}
            {#<a class="navbar-brand" href="{{ path('kelp.home') }}">Kelp</a>#}
        {#</div>#}
        {#<div id="navbar" class="navbar-collapse collapse">#}
            {#<ul class="nav navbar-nav">#}
                {#{% if is_granted("ROLE_USER") %}#}
                    {#<li {{ currentPath == path('kelp.dashboard.home') ? 'class="active"' : ''}}>#}
                        {#<a href="{{ path('kelp.dashboard.home') }}">Dashboard</a>#}
                    {#</li>#}
                {#{% endif %}#}
                {#{% if is_granted("ROLE_ADMIN") %}#}
                {#<li {{ currentPath == path('kelp.setting.type_storage') ? 'class="active"' : ''}}>#}
                    {#<a href="{{ path('kelp.setting.type_storage') }}">TypeStorage</a>#}
                {#</li>#}
                {#<li {{ currentPath == path('kelp.setting.user') ? 'class="active"' : ''}}>#}
                    {#<a href="{{ path('kelp.setting.user') }}">User</a>#}
                {#</li>#}
                {#{% endif %}#}
            {#</ul>#}
            {#<ul class="nav navbar-nav pull-right">#}
                {#<li class="dropdown">#}
                    {#<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"#}
                       {#aria-haspopup="true" aria-expanded="true">Gestion <span class="caret"></span></a>#}
                    {#<ul class="dropdown-menu">#}
                    {#<li {{ currentPath == path('index_type') ? 'class="active"' : ''}}><a href="{{ path('index_type') }}">Type</a></li>#}
                    {#<li {{ currentPath == path('index_genre') ? 'class="active"' : ''}}><a href="{{ path('index_genre') }}">Genre</a></li>#}
                    {#</ul>#}
                {#</li>#}
                {#<li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>#}
            {#</ul>#}
        {#</div>#}
    {#</div>#}
{#</nav>#}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Kelp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('kelp.home') }}">Dashboard<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('kelp.storage') }}">Storage</a>
            </li>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item dropdown pull-right">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    Administration
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">Type Storage</a>
                </div>
            </li>
        </ul>
    </div>
</nav>