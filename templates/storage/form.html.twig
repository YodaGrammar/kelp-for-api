{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-responsive right-block save" name="button">{{ 'storage.form.button.save'|trans }}</button>
{{ form_end(form) }}